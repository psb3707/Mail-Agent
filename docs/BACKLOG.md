# 백로그 — 해커톤 당일 이전에 판단할 것

## 1. 시연 질문 3번 (미이수 교육) 데이터 보강 — 보류

**현재 상태**: `〔러닝포털〕` 메일 8통에 **이수/미이수 구분과 마감일이 없어** 답이 나오지 않는다.
질문 1(견적)·2(버스)는 데이터상 완전히 성립한다.

**살리려면**: 러닝포털 메일 본문에 과정별 이수 상태와 마감일을 심고,
"학습시작 안내"(09-01) → "학습현황 안내"(09-08) 사이에 이수한 과정과 안 한 과정이 갈리게 한다.
`scripts/generate_data.py`의 `COURSES` 정의만 고치면 되고 30분이면 충분하다.

**가치**: "흩어진 자동알림 8통을 한 줄로 정리"는 공감대가 크다. 다만 질문 1·2로도 시연 3~4분은 채워진다.
**판단(2026-09-10)**: 진행 보류. 당일 리허설 뒤 가치 판단.

## 2~3. 결정 완료 (2026-09-10) — 더 이상 판단 항목 아님

- 해커톤 사전 코딩 규정 확인: **불필요** (PM/사용자 결정 — 작업이 모두 완성되어 제출되는 구조)
- 동료 8~10명 미니 설문: **불필요** (PM/사용자 결정)

## 4. 시연용 예비 질문 준비

심사에서 "그럼 이건 찾아주나요?"가 나온다는 전제로, 대본 밖 질문 3개를 미리 정해
당일 리허설에서 실제로 돌려본다. 현재 데이터로 답이 나오는 후보:
- "D-MIG 이관 리허설 얼마나 걸렸어?" → 리허설결과보고.pdf (6시간 42분)
- "고객채널 TF에서 뭐 결정됐지?" → 회의록_0312.pdf (우선순위 상위 5건, 2분기 착수)
- "ESG 공시 외부 검증 누가 했어?" → 최종안_v2.pdf (한국품질재단)

> **재검증 완료 (2026-09-10, 지시-016)**: 새 색인(9건·43통, `abada17`) 기준으로
> 위 3개 질문을 `app/search.py::answer_question()`으로 실제 호출해 확인했다.
> - ① D-MIG 리허설 → `D-MIG_리허설결과보고.pdf` · 근거 "총 소요: 6시간 42분" ✅
> - ② 고객채널 TF 결정 → `고객채널TF_회의록_0312.pdf` · 근거 "우선순위 상위 5건 선정, 2분기 착수" ✅
> - ③ ESG 검증 → `ESG공시_최종안_v2.pdf` · 근거 "외부 검증 완료 (검증기관: 한국품질재단)" ✅
>
> **동반 수정**: `app/search.py::answer_question()`이 LLM 답변에서 언급한 첨부 파일명을
> 찾아 `attachment`·`evidence`를 해당 문서 기준으로 갱신하도록 보강(기존엔 후보 top 1 고정).
> 검증 테스트 `test_backlog_q_*` 3건 추가, 전체 62건 PASS.
