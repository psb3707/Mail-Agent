# 로컬 진행 작업 감지 — 지시-010 준비 (프롬프트 중앙화)

> 2026-09-10 · 감시 주기
> 원격 신규 커밋: **없음** (`d9e7411` 유지, last_seen 정상)

## 감지

워크스페이스 작업 트리에서 다른 개발자 세션이 아래를 만들고 있다 (커밋 전).

| 파일 | 상태 | 내용 |
|---|---|---|
| `agent/prompts.py` | ?? (신규) | 프롬프트 중앙화 — SYSTEM_PROMPT·JSON_FORMAT_GUIDE·FALLBACK_REPLY·build_tool_prompt·build_result_prompt |
| `tests/test_prompts.py` | ?? (신규) | 프롬프트 계약 테스트 6건 |
| `agent/__init__.py` | M (수정) | prompts 모듈 export 추가 |

## 성격 (연결 지점)

- **지시-010(오케스트레이터)** 대비 준비: 프롬프트를 함수·상수로 중앙화해
  오케스트레이터가 import해 조립하는 계약만 정의.
- 기존 `agent/manager.py::_pick_tool` 규칙 스텁을, 이 프롬프트로 **LLM 기반 도구 선택**으로
  교체하는 것이 지시-010의 목표로 보임 (prompts.py docstring: "오케스트레이터(지시-010)가 쉽게 조립").
- `manager.py`는 수정하지 않는다고 명시 — **파일 충돌 위험 낮음**.

## PM 전달

1. 원격 변화 없음 — 기준 `d9e7411` 유지.
2. 로컬에 지시-010 준비 작업이 진행 중 (prompts 중앙화). 기존 agent 골격과 계약 호환.
3. 이 작업이 커밋되면 다음 감시에서 정식 분석 예정.
4. 참고: 이전 보고한 `test_build_index.py` 인코딩 회귀 4건은 **여전히 미수리** — push 전 확인 권고.