# 지시-007 시연 자산(D1-7) — 연결 지점 점검 결과

> 작성: 2026-09-10 · 감시 주기 결과
> 원격 신규 커밋: **없음** (`f9348c0` 유지, last_seen 정상)

## 로컬 신규 작업 (D1-7 시연 자산, 진행·완료)

| 파일 | 상태 |
|---|---|
| `data/demo_cache.json` | ✅ 폴백 답 2건 (견적·버스) |
| `docs/BACKLOG.md` | ✏️ §5에 D1-7 완료 기록 추가 |
| `slides/발표-뼈대.md` | ✅ 10분 발표 뼈대 (S0~S7) |

## 연결 지점 (기존 코드와 맞물림)

### 1. `demo_cache.json` ↔ `app/search.py` `_fallback()`
- `search.py`의 `_fallback(question)`은 `demo_cache.json`의 `answers[]`에서
  `{"question", "answer", "attachment", "evidence"}`를 읽는 규약.
- 백로그 §5에 기록된 형식과 **정확히 일치** — 즉, D1-3 질의 모듈과 D1-7 캐시가 이미 호환.
- 단, 캐시는 **폴백 전용**(라이브 기본)이므로, 리베이스 후 `search.py`가 이 규약을
  그대로 쓰는지 pytest로 확인 필요.

### 2. `slides/발표-뼈대.md` ↔ 시연 정답 세트(data/DESIGN.md)
- S3 질문 2건, S4 장면 4(a001 v3 최신본) — 정답 세트와 일치. 데이터 변경이 없으면 유효.

### 3. `agent/`(지시-008) ↔ `demo_cache.json`
- 백로그 §5: "관리 Agent 데모에서도 동일 규약 재사용 가능(단일 레포 통합 고려)"
- `ClassifierAdapter`가 indexed.json을 소비하는 구조와 이어짐 — 통합 시 캐시 규약 공유 가능.

## PM 전달 요약

1. 원격 신규 커밋 없음 — 기준 `f9348c0` 유지.
2. D1-7 산출물 3종이 로컬에 준비됨 — `demo_cache.json` 규약이 `app/search.py` 폴백과 **호환 확인**.
3. 리베이스(`--continue` 대기)가 여전히 남음 — 이제 `agent/`·`slide/`·`demo_cache.json`까지
   작업 트리에 쌓여 있어, 리베이스 완료 후 **전체 `pytest tests/ -v`** 로 회귀·조합 확인이 필수.
4. 백로그 §2(사전 코딩 규정)·§3(동료 미니 설문)은 여전히 미확인·미착수 — 당일 리스크로 PM 판단 요청.