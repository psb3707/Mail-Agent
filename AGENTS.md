# AGENTS.md — mail-agent 프로젝트 지침

본 문서는 이 저장소에서 작업하는 에이전트/개발자가 프로젝트를 이해하고
기존 패턴과 일관되게 작업하도록 돕는 진입 지침이다.
전체 설계와 해커톤 스토리는 반드시 다음을 참조한다.

- **✓ 새 세션은 먼저 온보딩을 읽는다**: `docs/onboarding.md` — 지금 상태·테스트 현황·다음 할 일
- 설계/스토리/발표: `docs/specs/2026-09-09-mail-agent-design.md`
- 데이터 설계(말투·시연 정답): `data/DESIGN.md`
- 데이터 스키마: `data/SCHEMA.md`
- 미해결 판단: `docs/BACKLOG.md`

## 1. 프로젝트 정체성

**한 줄 정의**
> 메일함을 도착순이 아니라 **건(件) 단위로** 보게 하는 레이어.

사내 메일함에 산재한 메일을 업무 맥락(건) 단위로 자동 재조립·분류해 주는 AI Agent.
사내 해커톤 출품작이며, 실제 사내 망에 붙일 수 없으므로 **가상 메일 시스템을 모킹해
에이전트 동작을 시연하는 PoC**로 진행한다.

**핵심 명제**
- 메일함은 보관만 하고 정리는 하지 않는다 → 정리 비용을 매번 사람이 낸다.
- 메일은 **통** 단위로 도착하는데, 일은 **건** 단위로 돌아간다.
- 통을 건으로 재조립하는 판단(같은 건인가 / 같은 문서의 어느 버전인가)은
  규칙으로 불가능하고 그래서 AI가 타당하다.

## 2. 현재 상태 (2026-09 기준)

**사전 준비(데이터) 완료, 애플리케이션 미착수.**

| 산출물 | 상태 |
|---|---|
| `data/mails.json` | ✅ 가상 메일 **400통** / 건 라벨(`A`,`B`,`C`,`BG01`~`BG06`) / 첨부 16개 |
| `data/attachments/` | ✅ 진짜 PDF·XLSX 16개 (한글 텍스트 추출 검증 완료) |
| `data/DESIGN.md` | ✅ 말투 계승 규칙, 건 구성, 시연 정답 세트 |
| `scripts/generate_data.py` | ✅ 재현 가능 (seed 고정 `20260910`) |
| `scripts/generate_attachments.py` | ✅ |
| `scripts/build_index.py` | ⬜ 미작성 |
| `app/` (FastAPI 단일 페이지) | ⬜ 미착수 (`app/` 빈 디렉토리만 존재) |
| `docs/BACKLOG.md` | ✅ 사전 코딩 규정 확인 · 동료 미니 설문 미착수 |

**유의**: 설계문서(100통, 9건)와 실제 데이터(400통, case 라벨 9종)의 수치는 다르다.
**실제 데이터가 기준**이며 문서의 옛 숫자는 참고용.

## 3. 기술 스택 (pyproject.toml 기준)

- Python **>=3.11**, 가상환경 `.venv`
- **FastAPI** + **uvicorn[standard]** — 백엔드/서버
- **Jinja2** — 단일 페이지 템플릿
- **anthropic** — Claude LLM 호출 (시연 라이브 호출)
- **pypdf** — PDF 텍스트 추출
- **openpyxl** — XLSX 읽기/생성
- **reportlab** — PDF 생성(첨부 모킹)

## 4. 아키텍처 원칙

설계문서가 명시적으로 밀어낸 것들 — **따르지 말 것**:
- **DB 없음** (100~400통에 SQLite는 순수 비용)
- **어댑터 추상화 없음** (데이터 소스가 하나뿐)
- **규칙 엔진 없음** (절감할 비용이 없음, AI 판단이 핵심)
- **화면은 단일 페이지 토글** (도착순 ↔ 건 단위, 구현량 절반)

**장면별 담당 모듈(계획)**
```
app/
  main.py        # FastAPI — 라우트 3개 (/, /ask, /versions)
  grouping.py    # 건 단위 재조립 (장면 2)
  search.py      # 자연어 질의 → 후보 → 근거 (장면 3)
  versions.py    # 같은 문서 버전 판별 + 차이 요약 (장면 4)
  llm.py         # Claude 호출 + 실패 시 캐시 폴백
  templates/index.html  # 단일 페이지
scripts/
  generate_data.py      # 가상 데이터 생성 (완료)
  generate_attachments.py # 가상 첨부 생성 (완료)
  build_index.py        # 첨부 추출 + 건 그룹핑 + 태깅 → indexed.json (미작성)
```

**핵심 동작 원칙**
- 장면 3·4는 **라이브 LLM 호출이 기본**, 캐시는 네트워크 장애 시 자동 폴백으로만.
- "왜 이것인가"가 데모의 심장 — 답만 내지 말고 **문서 안의 실제 값(금액·일자·버전·업체)을
  인용**해야 신뢰가 선다.

**동적 유입 원칙 (증분 정리·분류)**
- 분류는 1회성 배치가 아니라 "새 메일이 도착한 순간부터" 동작해야 한다. PoC는 전체
  배치로 실행하되 결과가 다음을 만족한다.
- **순서 무관**: 메일이 어떤 순서로 도착해도 같은 건으로 묶인다. 도착 시각·정렬은
  결정 근거가 아니라 보조 단서다. (`build_index.py`는 입력 순서를 뒤섞어도 같은
  `cases[]`가 나오는지 재현 테스트로 확인)
- **멱등 재색인**: 신메일이 들어와 전체를 다시 색인해도 기존 건 ID·결론이 흔들리지
  않는다. 건 ID는 시퀀스가 아니라 콘텐츠·발신자 기반의 안정 키를 쓴다.
- **증분 편입 3종**: 신메일은 ① 기존 건에 편입 ② 새 건으로 시작 ③ 비건 처리 중 하나로
  귀결된다. 어느 건에도 안 붙는 메일의 처리 규칙이 분류의 일부다.
- **저장은 인메모리**: 동적 구조는 세션/인메모리로만 동작, 영속화는 비범위(PoC 한정).

**비건(非件) 메일 처리 규칙** — 메일의 대부분(400통 중 357통)은 건으로 안 묶이는
공지·시스템 알림·개인 메일이다. 무시하지 않고 아래로 처리한다.

| 구분 | 처리 | 화면 표시 |
|---|---|---|
| 시연 건(A·B·C·BG01~06) | 사안 카드로 재조립 | 건 단위 뷰 |
| 반복 알림(교육·보안·근태) | 유형별 접기 | 하단 "알림" 섹션 |
| 단발성 공지·행정 | 무분류 유지 | "기타" 섹션 |

비건 처리는 "무엇이 건이고 무엇이 아닌가" 판단 자체를 시연 소재로 삼는다.

## 5. 데이터 모델

- `data/mails.json` → `{ "mails": [...], "attachments": [...] }`
  - mail: `id, subject, sender_name, sender_email, sender_dept, recipients, sent_at,
    body(로컬 전용), attachments[], reply_to, _case`
  - attachment: `id, filename, path, mime`
- 계획된 산출물 `data/indexed.json`:
  - `cases[]` — 건 단위 재조립 결과 (`id, title, mail_ids, attachment_ids, period, mail_type`)
  - `attachment_texts` — 첨부 추출 전문
  - `version_groups[]` — 같은 문서 버전 계열 (`doc, ids, latest`)

**설계 제약(데이터 성립 조건)**
- `subject`에 검색 키워드가 없는 메일을 의도적으로 포함 (장면 1 성립)
- 하나의 건이 여러 메일에 걸쳐 진행, `reply_to`가 끊긴 경우 포함
- 같은 문서의 버전 계열 3~4개 포함 (장면 4 성립)
- 말머리(`〔...〕`)·대소문자(`RE:`/`Re:`)·축약 표기 흔들림을 그대로 심음

## 6. 빌드 / 실행 / 데이터 재생성

```bash
# 가상환경 + 의존성
python3 -m venv .venv
./.venv/bin/pip install -e .

# 데이터 재생성 (재현 가능)
./.venv/bin/python scripts/generate_data.py
./.venv/bin/python scripts/generate_attachments.py

# 앱 실행 (app/ 구현 후)
./.venv/bin/uvicorn app.main:app --reload
```

## 7. 시연 시나리오 (4장면 고정)

| # | 장면 | 핵심 |
|---|---|---|
| 1 | 심사자에게 되묻기 | 실제 사내 메일 제목 30개 → "지난 분기 B사 견적서 어딨죠?" → 검색 실패 체감 |
| 2 | 건 단위 재조립 | 도착순 메일함이 사안 카드 + 타임라인으로 전환 |
| 3 | 첨부 정답 찾기 | 자연어 질문 → 문서 내용 근거로 정답 + "왜 이것인가" 인용 |
| 4 | 버전 판별(하이라이트) | 같은 문서 버전 계열 → 최신본 + 달라진 항목 |

**시연 정답 세트** (data/DESIGN.md 상세)
1. "N_CX 외주 견적 최종 얼마?" → `N_CX_UIUX_견적취합_v3.xlsx` ((주)디자인랩스 48,500,000원 VAT 별도, 2026-05-12 3차 취합본)
2. "워크숍 버스 몇 시 어디서?" → `NextW_버스배차_최종명단.pdf` (3호차 / 08:30 / 본사 정문)
3. "아직 안 들은 필수교육?" → 러닝포털 메일 8통 종합 (BACKLOG에 데이터 보강 보류 상태)

## 8. 세션 역할과 행동 규범 (자동 적용)

이 저장소에는 이름이 같지만 **역할이 다른 세션들이 함께 작업한다.**
세션은 아래 규범 중 **자신의 역할을 먼저 확인**하고, 해당 규범을 행동 기준으로 삼는다.
역할이 불명확하면 "개발자" 규범을 기본으로 한다.

### 8-A. 개발자(구현) 세션 — 기본값

**역할**: PM이 지시한 작업을 실제로 구현한다.

- **자동 준수** (이 섹션이 주입되므로 특별한 지시 없이도 지킨다):
  - 새 세션은 먼저 `docs/onboarding.md`를 읽고 지금 상태와 규칙을 파악한다.
  - 커밋은 덩어리 단위로, 해당 테스트 PASS 후에만 한다.
  - 구현 전 기존 패턴(말투·네이밍·파일 구조)을 확인하고 재사용한다.
  - 완료 시 `handoff.md`의 체크 표시를 최신화하고, 결과를 사용자에게 한국어로 보고한다.
- **금지**: 원본 데이터 수정, DB 도입, 새 폴더 임의 생성, 라이브 기본 경로를 캐시로 대체.

### 8-B. QA/검증자 세션

**역할**: 개발자 세션이 만든 결과를 검증하고 보고한다. **직접 구현·커밋·머지하지 않는다.**

- **자동 준수**:
  - 새 세션은 먼저 `docs/onboarding-qa.md`를 읽고 검증 기준을 파악한다.
  - 원격(GitHub) 신규 커밋 → 코드 구현 실제 상태 → 테스트 실행 → 기능 QA 순으로 검증한다.
  - 검증 기준은 시연 정답 3종과 설계 제약(원본 무수정·인메모리·라이브 기본)이다.
  - 결과는 `PM/DONE/QA검증-{YYYY-MM-DD}-{지시범위}.md`에 기록하고, 위키 상태 페이지를 갱신한다.
  - 결함(예: 테스트 실패)을 발견하면 수정하지 말고 **사실·재현·영향을 보고**한다.
- **금지**: 코드 수정, 커밋, 머지, 지시 생성.

### 8-C. 모두 공통

- **위키 정리**: 재사용 가능한 지식(의사결정·인사이트·절차)은 `wiki-organize` 스킬에 따라
  deepwork-mail 위키 컬렉션에 기록한다. 일회성 로그·PR 번호·임시 경로는 기록 금지.
- **한국어**: UI·답변·보고는 한국어 기본. 단, 커밋 메시지는 Windows 인코딩 문제를 피하려
  영문을 권장.
- **원본 무수정**: `data/mails.json`·`data/attachments/`는 누구도 수정하지 않는다.

## 9. 작업 시 지킬 것 (모든 세션 공통)

- 기존 패턴 재사용: 말투·네이밍·파일 구조를 새로 만들지 말고 유지.
- **절대 원본 메일함 데이터를 수정하지 않는다** — 시연은 "무수정"을 전제로 함.
- PoC지만 반출 보안 설계(화이트리스트)는 슬라이드에 포함 (발표용).
- 라이브 호출 의존 — 캐시는 폴백 전용이지 기본 경로가 아님.
- 새 기능 추가 시 설계문서의 "비범위" 항목(DB, OAuth, 증분 수집 영속화 등)을 되살리지 않는다.
  단, **동적 유입 판단 로직(순서 무관·멱등·증분 편입)** 자체는 지키는 것이 원칙이다.
