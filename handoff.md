# mail-agent — Handoff (총괄 PM 관점)

> 이 문서는 프로젝트를 이어받는 사람/에이전트가 **DEPTH 1 단계의 작업 덩어리**와
> 현재 상태를 한눈에 파악하고 바로 작업을 시작할 수 있게 하는 핸드오프다.
> 상세 설계·스토리는 `docs/specs/2026-09-09-mail-agent-design.md`,
> 단계별 구현 계획은 `docs/plans/2026-09-10-mail-agent-poc.md`를 따른다.

---

## 1. 프로젝트 한 줄 정의

> 메일함을 도착순이 아니라 **건(件) 단위**로 보게 하는 레이어.

- 메일은 **통(thread)** 단위로 도착하는데 일은 **건(件)** 단위로 돌아간다.
- 메일함은 보관만 하고 정리는 하지 않으며, 그 정리 비용을 매번 사람이 낸다.
- 통을 건으로 재조립하는 판단(같은 건인가 / 같은 문서의 어느 버전인가)은 규칙으로
  불가능해서 AI가 타당하다.
- 사내 해커톤 출품작 — 가상 메일 400통을 모킹해 동작을 시연하는 PoC.

---

## 2. 현재 상태 스냅샷 (커밋 `72906ac` 기준)

| 영역 | 상태 | 비고 |
|---|---|---|
| 가상 데이터 `data/mails.json` | ✅ 완료 | 메일 400통 · 건 라벨 9종 · 첨부 16개 |
| 가상 첨부 `data/attachments/` | ✅ 완료 | 진짜 PDF·XLSX, 한글 텍스트 추출 검증됨 |
| 데이터 설계 `data/DESIGN.md`·`SCHEMA.md` | ✅ 완료 | 말투 계승 규칙 · 시연 정답 세트 |
| 색인 파이프라인 `scripts/build_index.py` | ✅ 완료 | `cases: 9 / non_cases: 357 / versions: 1` → `data/indexed.json` 재생성 |
| 색인 테스트 `tests/test_build_index.py` | ✅ 완료 | 전체 9건 정합성 · 라벨 비의존 · 순서 무관 · 멱등 · 첨부/버전 (8건) |
| **앱 서빙 `app/`** | ✅ 구현 | 재조립·질의·LLM·버전·FastAPI 단일 페이지, 전체 테스트 통과 |
| 시연·발표 자산 `slides/` | ⚠️ 진행 중 | 발표 뼈대 생성, 리허설 필요 |
| 폴백 캐시 `data/demo_cache.json` | ✅ 완료 | 시연 정답 캐시 |
| 백로그 판단 `docs/BACKLOG.md` | ⚠️ 부분 | 사전 코딩 규정 확인·동료 미니 설문 미착수 |

**핵심 진단**: 데이터·색인·앱 계층은 구현됐다. 남은 핵심은 발표 자료 완성,
오프라인 폴백 포함 4장면 리허설, 대본 밖 질문의 답 품질 확인이다.

---

## 3. DEPTH 1 작업 분류 — 큰 덩어리 7개

DEPTH 1 = PoC가 "검색이 안 되는 메일함 → 건 단위 레이어"로 동작할 수 있을 때까지의 전 작업.
각 덩어리는 **"~ 구축"** 단위로 분류하며, 완료 조건이 명시되어 있다.

### D1-1 색인·데이터 파이프라인 구축 — ✅ 완료
- **산출물**: `scripts/build_index.py` + `tests/test_build_index.py` + `data/indexed.json`
- **내용**: 회신 그래프 기반 초기 그룹핑, 표기·생애주기·시간 근접성에 따른 끊긴 메일 편입,
  안정 키 건 ID, 첨부 텍스트 추출, 최신 버전 탐지, 비건(alert/misc) 분류.
- **완료 조건**: 실제 9건/43통과 비건 357통 정합, `_case` 라벨 비의존, 순서 무관·멱등 테스트 통과.
- **이어받을 때 손댈 일**: 신메일 재분류 시 `indexed.json`은 **읽기 전용**으로 유지.

### D1-2 건 재조립 에이전트 구축 — ✅ 완료 (플랜 Task 4)
- **산출물**: `app/grouping.py` + `tests/test_grouping.py` + `app/classify_rules.py`
- **내용**: ① 사전 계산 결과를 건 카드+타임라인으로 재조립(`reassemble(indexed)`)
  ② 신메일 1통의 증분 편입 판단(`classify_new_mail(new_mail, indexed, llm_call)`) —
  **3분기 상수** 반환: `existing`(기존 건 편입) / `new_case`(새 건 후보) / `non_case`(비건).
  분류 규칙(정규화·대표어·시간 근접·학습 캠페인)은 `app/classify_rules.py` 단일 출처로
  배치(`scripts/build_index.py`)와 공유 — 복제 드리프트 방지. LLM 프롬프트는 건 제목뿐 아니라
  대표어·기간 요약을 제공. 시스템 발신 알림은 LLM보다 먼저 비건 확정(라이브 LLM 오염 방지).
- **완료 조건**: LLM 없이(토큰 폴백으로도) 신메일 배정이 결정되는 테스트 통과 + 400통 전수 감사 400/400 정합.

### D1-3 자연어 질의·근거 에이전트 구축 — ✅ 완료 (플랜 Task 2)
- **산출물**: `app/search.py` + `tests/test_search.py`
- **내용**: 질문에서 단서 추출(LLM 1회) → 후보 축소(`indexed.json` 필터·키워드) →
  정답 선택 + **문서 속 실제 값 인용**(LLM 1회).
- **완료 조건**: "N_CX 외주 견적 최종 얼마" → `N_CX_UIUX_견적취합_v3.xlsx`,
  (주)디자인랩스 48,500,000원(VAT 별도) 근거 포함 답변, `cached=False`.
- **의존성**: D1-1 → D1-4.

### D1-4 LLM 게이트웨이(호출·폴백) 구축 — ✅ 완료 (플랜 Task 3)
- **산출물**: `app/llm.py` + `tests/test_llm.py`
- **내용**: Claude(anthropic) 라이브 호출이 **기본 경로**, 네트워크 장애 시
  `data/demo_cache.json` 사전 계산 답변으로 **자동 폴백**(캐시는 기본 경로 아님).
- **완료 조건**: 호출 실패 시 캐시 폴백이 동작하는 테스트 통과.
- **의존성**: 없음(가장 먼저 만들 수 있는 덩어리).

### D1-5 버전 판별 에이전트 구축 — ✅ 완료 (플랜 Task 5)
- **산출물**: `app/versions.py` + `tests/test_versions.py`
- **내용**: 같은 문서 버전 계열 → 최신본 + 달라진 항목(단가·일자·업체) 요약.
- **완료 조건**: 버전 계열 `ids`에서 최신본을 골라 "달라진 것"을 설명하는 테스트 통과.
- **의존성**: D1-1(버전 그룹) → D1-4.

### D1-6 웹 서빙·단일 페이지 구축 — ✅ 완료 (플랜 Task 6)
- **산출물**: `app/main.py` + `app/templates/index.html` + `app/__init__.py`
- **내용**: FastAPI 라우트 3개(`/`, `/ask`, `/versions`), Jinja2 단일 페이지,
  도착순↔건 단위 토글, 건 카드(타임라인+첨부 위치), 질문창+근거 표시, 비건
  "알림"/"기타" 섹션, 인메모리 저장(영속화 없음).
- **완료 조건**: `uvicorn app.main:app` 기동 → `/`에서 카드·토글·질문·버전 표 확인.
- **의존성**: D1-2~D1-5 전부.

### D1-7 시연·발표 자산 구축 — ⚠️ 진행 중 (플랜 Task 7 + BACKLOG)
- **산출물**: `data/demo_cache.json`(폴백용 사전 계산 답변 2건) · `slides/` · 리허설 자료
- **내용**: 4장면 대본 정합(장면 2 시작에 **정리 시연 1분** 배치 — 문제 규정 "정리"와
  시연 일치), 대본 밖 예비 질문 3개 리허설, 사전 코딩 규정 확인, 동료 미니 설문.
- **완료 조건**: 오프라인 상태에서도 데모 4장면이 진행되고, 임의 질문에 답하는 리허설 통과.
- **의존성**: D1-6 이후 + 발표 10분 리허설.

---

## 4. 전역 제약 (모든 덩어리에 적용)

| 제약 | 내용 |
|---|---|
| 원본 무수정 | `data/mails.json`·`data/attachments/` 절대 수정 금지 (시연 "무수정" 전제) |
| 저장 인메모리 | DB(SQLite) 금지 — 동적 구조는 세션/인메모리만, 영속화 비범위 |
| 라이브 기본·캐시 폴백 | LLM 라이브 호출이 기본, 캐시는 장애 시 자동 폴백 전용 |
| 순서 무관·멱등·증분 편입 | 분류 결정 근거는 내용, 시각은 보조 단서. 재실행해도 건 ID·결론 불변 |
| 비건 처리 | 건에 안 붙는 메일(357통)을 무시하지 않고 "알림"/"기타"로 표시 |
| 근거 인용 | 답만 주지 말고 문서 속 실제 값(금액·일자·버전·업체)을 인용 |
| 구조 유지 | 파일 구조는 설계문서의 `app/`·`scripts/` 구성 그대로 (새 폴더 X) |
| AI 경계 | 규칙으로 되는 판단(정규화·토큰·추출)은 규칙, 규칙 불가 판단(같은 건·버전)만 AI |

---

## 5. 작업 순서 (의존성 기준)

```
D1-4 LLM 게이트웨이 (기반 모듈, 선행 가능)
   ↓
D1-2 재조립 ─┐
D1-3 질의   ─┼─→ D1-6 웹 서빙(통합) → D1-7 시연·발표
D1-5 버전   ─┘        (D1-1은 완료, 읽기 전용 입력)
```

- **가장 먼저**: D1-4(llm.py) — 다른 덩어리들이 의존하는 공통 모듈.
- **가장 늦게**: D1-7 — D1-6이 뜬 뒤 리허설로 검증.
- 병렬 가능: D1-2 / D1-3 / D1-5 (각자 `indexed.json` + llm_call 인터페이스만 알면 독립).

---

## 6. 다음 핸드오프에 넘길 것

1. 커밋 하나당 한 덩어리 (예: `feat: ...`) — 플랜 문서의 커밋 메시지 컨벤션 유지.
2. 덩어리 완료 시 해당 테스트(`tests/test_*.py`) PASS 확인 후 진행.
3. `handoff.md`의 체크 표시(✅/⬜)를 최신 상태로 갱신.
4. DEPTH 1 완료 기준 = 4장면 시연 전체가 오프라인 폴백으로도 돌아가는 상태.

## UI 리디자인 — 2026-09-10

- ✅ 밝은 워크스페이스 UI 및 루트 DESIGN.md 구축.
- ✅ 400통 도착순 제목 검색, 업무 타임라인 모달, 파일명 기반 버전 비교, AI 질문 패널, 알림/기타, 신규 분류 화면.
- ✅ 기존 테스트 75개 통과 (pytest 임시 경로는 작업 폴더 지정). 브라우저에서 검색 4통, 타임라인, 48,500,000원 근거 답변, 버전 비교 및 기존 업무 편입 검증.
- 라이브 API 키 미설정 환경으로 AI 답변은 폴백 경로 검증. 원본 데이터와 백엔드 판단 경로 유지.

## AI 답변·오케스트레이터 개선 — 2026-09-10

- ✅ Markdown 제목·강조·목록·표 렌더링, 핵심 요약 카드, 출처 접기, 크게 보기.
- ✅ `/ask` → 실제 mailbox_skills → ToolHarness → Orchestrator → 구조화된 응답 연결. 매 요청 별 인메모리 컨텍스트로 사용자 간 대화 혼합 방지.
- ✅ agent/prompts.py의 해요체·직접 답변·사실/추측 구분 지침을 검색과 오케스트레이터에서 공통 사용.
- ✅ 최근 메일 요청은 수신 시각 기준 최신 12통과 기간 표시. 무관한 첫 첨부를 반환하던 후보 폴백 제거.
- ✅ 도구 필수 인자·타입 검증, 예시 ID 기본값 제거, 동일 호출 재실행 방지, 최대 단계 이후 추가 도구 호출 제거.
- ✅ Docker 이미지에 agent 패키지 포함, Markdown 라이브러리의 원시 HTML·이미지 비활성화.
- 검증: 기능·회귀·라우트 테스트 87개 통과. 브라우저 시각 검증은 도구 사용량 제한으로 미완료.
- 배포는 Render API로만 실행·확인한다. API 키는 저장하지 않는다.

## 메일 바로가기 — 2026-09-10
- ✅ 타임라인 및 AI 참고 메일 제목에 /#mail-ID 링크 적용.
- ✅ 답변 본문 메일 인용 링크 지원, 실제 메일 ID 검증 및 원문 제목으로 출처 정규화.
- ✅ 이동 시 모달 닫기·검색 초기화·본문 펼치기·선택 강조·키보드 포커스. 새 탭/새로고침에서도 동작.
- ✅ 폴백 첨부 답변에도 해당 문서를 포함한 실제 메일 출처 연결. 회귀 테스트 89개 통과.
- ✅ 브라우저에서 타임라인 → 메일 원문, 새로고침 후 같은 메일 유지, 확대 답변 출처 → 메일 이동 확인.
